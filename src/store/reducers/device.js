const initialState = {
    id:         3,
    pointId:    '3',
    buildingId: 2168
};

const reducer = (state = initialState, action = {}) => {
    switch (action.type) {

        default:
            return state
    }
};

export default reducer;